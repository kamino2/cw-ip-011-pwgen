#pragma once

#include <iostream>
#include <string>

using namespace std;

int generation_alphabet(
        string* alphabet,
        int* length_alphabet,
        bool digit_flag,
        bool small_character_flag,
        bool large_character_flag,
        bool special_symbol_flag);