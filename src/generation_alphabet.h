#pragma once

#include <iostream>
#include <string>

using namespace std;

int generation_alphabet(
        string* alphabet,
        int* length_alphabet,
        bool digit_flag,
        bool small_symbol_flag,
        bool large_symbol_flag,
        bool special_symbol_flag);