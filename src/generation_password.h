#pragma once

#include <iostream>
#include <string>

using namespace std;

int generation_password(
        string** password,
        string alphabet,
        int length_alphabet,
        int quantity_password,
        int length_password);